import Vue from 'vue'
import App from './App.vue'
import snakeToTitle from './utils/snakeToTitle.js'
import numberString from './utils/numberString.js'

new Vue({
  el: '#app',
  render: h => h(App)
})
